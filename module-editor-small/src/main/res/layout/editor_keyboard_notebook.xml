<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <RelativeLayout
    android:id="@+id/dialog_add_task_select_ll_notebook"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/master_background"
    android:orientation="vertical">

    <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/select_rv_horizontal_notebook"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_above="@+id/add_notebook"
      android:layout_alignParentTop="true"
      tools:listitem="@layout/base_list_item"/>

    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="0.5dp"
      android:layout_above="@+id/add_notebook"
      android:background="@color/blue"/>

    <TextView
      android:id="@+id/add_notebook"

      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:layout_alignParentBottom="true"
      android:gravity="center"
      android:text="新建文集"
      android:textColor="@color/blue"
      android:textSize="12sp"/>
  </RelativeLayout>
</merge>